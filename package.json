{
  "name": "lark-linked-booking-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check src/env.js && node --check src/http.js && node --check src/storage.js"
  },
  "dependencies": {
    "nodemailer": "^8.0.7",
    "pdfkit": "^0.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
